请继续实现 CRM 系统“系统管理 → 用户管理 → 用户列表”功能。

当前已经完成：

后端：
- Spring Security 用户名密码登录
- JWT Access Token 签发
- JwtAuthenticationFilter
- SecurityContext 身份恢复
- POST /api/auth/login
- 除登录接口外，其他接口默认 authenticated()

前端：
- Vue3 + TypeScript
- 登录页已完成前后端联调
- 登录成功后 JWT 保存到 localStorage
- Axios 请求拦截器会自动添加：
  Authorization: Bearer <JWT>
- 已有后台首页和系统管理菜单
- 已有“用户管理”页面或菜单入口，请先检查当前实际代码
- 当前用户管理页面如果仍使用前端写死的 users 数组，需要替换成后端真实数据

数据库已经准备好测试数据：

- admin
- zhangsan
- lisi
- wangwu
- zhaoliu
- sunqi

并已经存在相应：
- sys_user
- sys_role
- sys_user_role
数据。

本阶段只实现“用户列表查询和展示”。

不要实现：
- 新增用户
- 编辑用户
- 删除用户
- 重置密码
- 分配角色
- RBAC 权限控制
- 分页
- 搜索
- 筛选
- /auth/me
- Refresh Token

-----------------------------------
一、后端接口
-----------------------------------

实现：

GET /api/users

注意：
后端已有 context path /api，
Controller 内部路径不要重复写 /api。

该接口必须是受保护接口。

不要在 Controller 中手动解析 JWT。

请求应自然经过：

Authorization: Bearer <JWT>
↓
JwtAuthenticationFilter
↓
SecurityContext
↓
UserController
↓
UserService
↓
Mapper
↓
数据库

如果 JWT 无效或缺失，继续使用现有 Spring Security 机制返回 401。

-----------------------------------
二、后端分层
-----------------------------------

请先检查当前项目是否已经存在：

- SysUser.java
- SysUserMapper.java
- SysRole.java
- SysRoleMapper.java
- SysUserRole.java
- SysUserRoleMapper.java
- UserController / UserService 等相关类

如果已有，优先复用。
不要重复创建功能相同的类。

建议结构：

UserController
↓
UserService
↓
UserServiceImpl
↓
SysUserMapper / 相关 Mapper

如果当前项目 Service 命名规范不同，以现有风格为准。

-----------------------------------
三、用户列表返回字段
-----------------------------------

前端当前页面需要展示类似：

{
  id: 1,
  username: "admin",
  name: "系统管理员",
  role: "超级管理员",
  phone: "13800000000",
  status: "正常",
  createTime: "2026-08-03"
}

请根据真实数据库字段生成 VO。

建议新增：

UserListVO.java

字段名称优先与前端真实页面一致，或者统一采用后端风格后同步修改前端类型。

至少包含：

- id
- username
- name
- role
- phone
- status
- createTime

但不要猜数据库字段名。

请先检查：
- SysUser 中真实姓名字段到底叫 name、realName、nickname 还是其他
- 创建时间字段实际名称
- status 实际值定义
- sys_role 中角色名称字段

-----------------------------------
四、角色查询
-----------------------------------

用户列表中的 role 必须是真实数据库角色。

需要根据：

sys_user
↓
sys_user_role
↓
sys_role

获取角色名称。

当前测试用户主要是：

- admin → 超级管理员
- zhangsan → 销售人员
- lisi → 销售经理
- wangwu → 销售人员
- zhaoliu → 销售人员
- sunqi → 销售经理

请根据当前真实表结构选择合适实现。

可以：
- 使用 Mapper 联表查询；
或
- 使用已有 Mapper 分步查询。

对于当前小项目，优先选择代码清晰、容易理解的方案，不做复杂抽象。

如果一个用户可能有多个角色，请先检查当前业务设计。

本阶段用户列表页面如果只展示一个 role 字段：
- 可以按当前测试数据返回单个角色；
- 不要为了多角色场景过度设计。

-----------------------------------
五、状态转换
-----------------------------------

数据库中的 status 很可能是数字。

前端希望展示：

正常
停用

请在后端或前端选择一个明确位置做转换。

优先建议后端 VO 直接返回适合展示的状态文本：

正常
停用

这样前端不需要知道数据库中 0 / 1 的具体含义。

但请先检查项目当前 status 定义，不要猜。

-----------------------------------
六、前端 API
-----------------------------------

建议新增或完善：

src/api/user.ts

例如：

export interface User {
  id: number
  username: string
  name: string
  role: string
  phone: string
  status: string
  createTime: string
}

export const getUserList = () =>
  request.get<User[]>('/users')

注意：

request.ts 已配置：

baseURL: /api

所以前端只写：

/users

最终请求：

GET /api/users

不要写成：

/api/users

否则可能产生：

/api/api/users

-----------------------------------
七、前端用户管理页面
-----------------------------------

请找到当前“系统管理 → 用户管理”页面。

如果当前存在：

const users = ref<User[]>([
  ...
])

这种写死数据，请删除或替换。

改为：

页面加载
↓
调用 getUserList()
↓
response.data
↓
赋值给 users
↓
表格展示

保留现有页面 UI 和表格样式。

不要重构页面布局。

增加：

- loading 状态
- 请求失败提示
- 空数据状态（如果当前 UI 组件支持）

-----------------------------------
八、JWT 验证链路
-----------------------------------

不要在用户页面手工读取 Token。

前端请求：

getUserList()
↓
request.ts
↓
自动读取 localStorage
↓
自动添加：
Authorization: Bearer <JWT>

后端：

JwtAuthenticationFilter
↓
验证 JWT
↓
恢复 SecurityContext
↓
GET /users
↓
查询数据库

本阶段请特别验证这一点。

-----------------------------------
九、测试重点
-----------------------------------

请完成以下验证：

1. 正常登录 admin
2. 进入“系统管理 → 用户管理”
3. 浏览器 Network 中看到：

GET /api/users

4. Request Headers 中存在：

Authorization: Bearer <JWT>

5. 后端返回 200
6. 页面展示数据库中的真实测试用户
7. 用户角色来自 sys_role，而不是前端写死
8. wangwu 显示为“停用”
9. 删除 localStorage 中的 crm_access_token 后重新访问用户管理：
   → 401 或路由返回登录页
10. 手工篡改 JWT 后重新请求用户列表：
   → 后端返回 401
   → 前端清除无效 Token
11. 前端 build 成功
12. 后端 test / compile 成功

-----------------------------------
十、特别实验
-----------------------------------

为了帮助理解 JWT，请在最终报告中特别说明：

正常 Token：

点击用户管理
→ GET /api/users
→ Authorization: Bearer JWT
→ JwtAuthenticationFilter 验证成功
→ UserController
→ 数据库
→ 200

篡改 Token：

点击用户管理
→ GET /api/users
→ Authorization: Bearer 被篡改的 JWT
→ JwtAuthenticationFilter 验证失败
→ 不进入 UserController
→ 401

请明确说明是否通过真实测试验证。

-----------------------------------
十一、不要执行
-----------------------------------

不要执行：

git commit
git push

不要修改与用户列表无关的模块。

-----------------------------------
十二、完成后输出
-----------------------------------

请输出：

1. 新增文件
2. 修改文件
3. GET /api/users 完整调用链
4. 用户列表 SQL / Mapper 查询方式
5. sys_user、sys_user_role、sys_role 如何组合出角色名称
6. 后端 UserListVO 字段
7. 前端用户列表页面如何从写死数据改成真实接口
8. Axios 如何自动携带 JWT
9. 正常 JWT 测试结果
10. 篡改 JWT 测试结果
11. 401 后前端如何处理
12. 构建与测试结果
13. 下一阶段建议

优先保持当前项目结构和代码风格。
本阶段目标只有一个：让用户管理页面真实展示数据库中的用户列表，并通过现有 JWT 认证保护这个接口。