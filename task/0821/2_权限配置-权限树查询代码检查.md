请只读检查本次权限树接口相关代码，不要修改任何文件。

重点检查以下文件：

backend/src/main/java/com/company/crm/controller/PermissionController.java
backend/src/main/java/com/company/crm/entity/SysPermission.java
backend/src/main/java/com/company/crm/mapper/SysPermissionMapper.java
backend/src/main/java/com/company/crm/service/PermissionService.java
backend/src/main/java/com/company/crm/service/impl/PermissionServiceImpl.java
backend/src/main/java/com/company/crm/vo/permission/

背景：

当前已经实现：

GET /api/permissions

功能是：
从 sys\_permission 查询启用权限，
根据 id / parent\_id 组装权限树，
返回 PermissionTreeVO。

接口已经通过 Postman 测试。

请只进行代码审查，不要自动修改。

重点检查：

1. Controller / Service / Mapper 分层是否合理
2. SysPermission Entity 与 sys\_permission 表字段映射是否完整
3. PermissionTreeVO 字段设计是否合理
4. Entity → VO 转换是否规范
5. 权限树构建逻辑是否存在 bug
6. HashMap 遍历是否可能导致 sort\_order 顺序丢失
7. parentId 判断是否存在 Long 比较问题
8. children 是否存在空指针风险
9. Mapper 查询是否符合 MyBatis-Plus 使用规范
10. 依赖注入方式是否符合当前项目风格
11. 是否存在重复 import、无用 import、无用注解
12. 命名和包结构是否与现有 User / Role 模块保持一致
13. 是否需要补充单元测试
14. 是否存在当前接口能跑通但后续扩展 RBAC 时容易产生问题的设计

输出：

1. 必须修改的问题
2. 建议修改的问题
3. 当前可以保留的问题
4. 推荐的最小修改清单
5. 是否建议在修改后再提交 Git

不要修改代码，只输出审查报告。