请在当前 CRM 前端项目中实现“系统管理 → 角色管理”第一版前端页面。

当前只实现前端页面和交互骨架，不接后端接口，不使用真实数据，不修改后端代码。

技术栈：
Vue3
Vite
TypeScript
Element Plus


现有页面：
src/views/system/User.vue


角色管理页面整体风格、布局、间距、搜索区域、表格区域、分页区域、按钮风格，应尽量与 `system/User.vue` 保持一致。

目标功能结构：
角色管理
├── 角色列表
├── 新建角色
├── 编辑角色
└── 配置权限


当前阶段只做 UI 和基础交互。

角色列表数据先保持为空，不写死任何测试角色数据，也不要调用后端接口。

一、先检查现有项目结构

修改前先检查：
src/views/system/User.vue
src/router/index.ts
src/layout/
src/components/


优先复用现有布局、搜索栏、表格、分页、Dialog 等组件风格。

如果当前用户管理已经拆分成：
UserSearch.vue
UserTable.vue
UserDialog.vue


可以参考其组件化方式，但不要为了完全一致而进行大规模重构。

二、新增角色管理页面

优先新增：src/views/system/Role.vue

如果当前路由已经有角色管理占位页面，则直接修改现有文件，不重复创建。

页面标题：角色管理

页面整体结构参考 `User.vue`。

三、角色列表字段

第一版角色列表建议包含：
角色名称
角色编码
状态
权限数量
备注
操作


对应 TypeScript 类型可以类似：
```ts
interface Role {
  id: number
  roleName: string
  roleCode: string
  status: number
  permissionCount: number
  remark?: string
}
```

当前数据：
```ts
const roles = ref<Role[]>([])
```

必须保持为空。

不要写：
```ts
const roles = ref([
  { roleName: '超级管理员', ... }
])
```

因为后续会通过真实后端接口加载数据库数据。

空数据时使用 Element Plus 表格默认空状态，或者显示简洁的：暂无角色数据

四、顶部搜索区域

参考 `User.vue` 的搜索样式。

第一版搜索条件：
  角色名称
  角色编码
  状态


状态选项：
  全部
  启用
  禁用

提供：
  查询
  重置
按钮。

当前因为数据为空，可以先实现前端筛选逻辑骨架。

不要接 API。

五、主要操作按钮

搜索区域或表格上方增加：+ 新建角色

点击后打开角色编辑 Dialog。

六、新建角色 Dialog

Dialog 标题：新建角色

表单字段：
  角色名称
  角色编码
  状态
  备注

建议：
角色名称：
例如：销售经理

角色编码：
例如：SALES_MANAGER

状态：
启用 / 禁用


备注：
角色用途说明


底部按钮：
取消
确定


当前阶段点击“确定”不要真正写入数据，也不要调用接口。

可以：
ElMessage.info('角色新增功能待后端接口完成后接入')


然后关闭或保持 Dialog 均可，但请保持交互自然。

七、编辑角色

角色列表“操作”列提供：
编辑

点击后复用同一个 Dialog。

Dialog 标题改为：
编辑角色


如果将来有真实行数据，应自动回填：
  roleName
  roleCode
  status
  remark


当前列表为空，因此只实现方法和结构。

建议保留：
```ts
handleCreateRole()
handleEditRole(role)
```

八、配置权限

操作列增加：配置权限

点击后打开权限配置 Dialog。

第一版权限配置 Dialog 可以先做成“权限树/分组展示”的 UI 骨架。

建议按模块展示：
```text
系统管理
├── 用户管理
│   ├── 查看用户
│   ├── 新建用户
│   ├── 编辑用户
│   └── 删除用户
│
└── 角色管理
    ├── 查看角色
    ├── 新建角色
    ├── 编辑角色
    ├── 删除角色
    └── 配置角色权限
```

权限编码参考当前已经设计的：
```text
user:list
user:create
user:update
user:delete
user:assign_role

role:list
role:create
role:update
role:delete
role:assign_permission
```

这里可以使用：
```text
el-tree
```

并启用 checkbox。

当前这些权限节点可以作为 UI 配置常量存在，因为它们属于前端权限配置页面结构，不是角色列表测试数据。

但不要假装这些权限已经从后端加载。

建议代码中明确注释：
TODO: 后续由 GET /permissions 或角色权限接口动态加载


Dialog 底部：
取消
保存权限


当前点击“保存权限”只提示：权限配置功能待后端接口完成后接入

不要真正保存。

九、操作列

第一版操作建议：
编辑
配置权限


暂时不要实现删除角色。

因为后续系统内置角色是否允许删除还需要进一步设计。

如果当前 `User.vue` 操作列使用按钮链接风格，则角色管理保持一致。

十、状态展示

状态建议：
1 → 启用
0 → 禁用


表格中使用 `el-tag`：
启用
禁用


风格参考 `User.vue`。

十一、分页

保持与 `User.vue` 相同的分页样式。

即使当前：roles = []

也保留分页结构，后续后端角色列表接口完成后直接接入。

十二、路由

检查当前系统管理路由。

确保：/system/role

可以正常进入 `Role.vue`。

如果已有对应路由，只复用，不重复创建。

如果没有，则补充最小必要路由配置。

不要修改其他菜单结构。

十三、页面视觉要求

整体视觉必须参考现有 `system/User.vue`：
相同页面背景
相同卡片布局
相同搜索栏高度
相同表格样式
相同按钮规格
相同分页位置
相同 Element Plus 设计语言

不要重新设计一套完全不同的角色管理 UI。

十四、当前阶段不要做

不要：
调用 /api/roles
调用 /api/permissions
调用角色权限保存接口
写死角色测试数据
修改后端
实现 @PreAuthorize
实现前端菜单权限隐藏
实现 Pinia 权限 Store
实现真实删除角色
大规模重构 User.vue


当前任务的目标只是：先把角色管理页面骨架设计完整

十五、建议页面最终结构
```text
角色管理

[搜索区域]
角色名称 [      ]
角色编码 [      ]
状态     [全部▼]

[查询] [重置]

[+ 新建角色]

--------------------------------------------------
角色名称 | 角色编码 | 状态 | 权限数量 | 备注 | 操作
--------------------------------------------------
暂无角色数据
--------------------------------------------------

                         分页
```

操作列后续：编辑 | 配置权限


十六、完成后验证

请验证：

1. `/system/role` 可以正常进入
2. 页面风格与 `User.vue` 基本一致
3. 列表为空时页面布局正常
4. 搜索区域正常显示
5. 点击“新建角色”可以打开 Dialog
6. 点击“编辑”方法结构已预留
7. “配置权限”Dialog 可以正常打开
8. 权限树 checkbox 可以正常交互
9. 不调用任何后端角色接口
10. TypeScript 编译通过
11. Vite 构建通过

完成后输出：
1. 新增/修改文件列表
2. Role.vue 页面结构
3. 新建/编辑角色 Dialog 设计
4. 权限配置 Dialog 设计
5. 路由是否调整
6. 哪些地方明确保留 TODO 等待后端接口
7. TypeScript / build 验证结果
