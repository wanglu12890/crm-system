请只补充权限模块自动化测试，不修改生产代码。

新增：

PermissionServiceImplTest
PermissionControllerTest

重点覆盖：

1. 空权限列表返回空树
2. 单根节点
3. 多级父子树
4. 同父节点按照 sortOrder、id 排序
5. 多个根节点顺序
6. children 始终非 null
7. 孤儿节点按照当前实现被忽略
8. parentId=0 正确识别根节点

Controller 测试覆盖：

1. 未认证访问 GET /api/permissions 返回 401
2. 已认证访问返回 200
3. JSON 中包含 id、parentId、permissionCode、permissionName、permissionType、routePath、sortOrder、children
4. children 为正确的嵌套结构

参考当前项目已有 AuthControllerTest、RoleControllerTest、User 相关测试风格。

不要修改生产代码，不要重构现有测试框架。

完成后运行 Maven 测试，并输出：

- 新增测试文件
- 每个测试覆盖场景
- 测试结果
- 是否发现生产代码潜在问题