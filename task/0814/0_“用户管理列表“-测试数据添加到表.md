请根据当前 CRM 项目的真实数据库表结构，为“用户管理列表”准备测试数据 SQL。

先只读检查：

1. init.sql 中：
   - sys_user
   - sys_role
   - sys_user_role
   三张表的真实字段结构、主键、自增、唯一约束、默认值。

2. 检查 DataInitializer.java：
   - 当前 admin 超级管理员如何初始化；
   - admin 当前对应的角色是什么；
   - 不要重复创建 admin 用户。

然后生成一个独立的开发测试数据 SQL 文件，例如：

backend/src/main/resources/db/dev-user-test-data.sql

本次需要准备以下用户：

1. 
username: admin
name: 系统管理员
phone: 13800000000
status: 正常
createTime: 2026-08-03

注意：
admin 已经由 DataInitializer 初始化。
不要重新 INSERT admin。
如果现有 admin 缺少 name、phone 等测试展示字段，可以根据真实表结构决定是否 UPDATE，但不要修改其密码和现有核心认证信息。

2. 
username: zhangsan
name: 张三
role: 销售人员
phone: 13900000000
status: 正常
createTime: 2026-08-03

3. 
username: lisi
name: 李四
role: 销售经理
phone: 13700000000
status: 正常
createTime: 2026-08-02

4. 
username: wangwu
name: 王五
role: 销售人员
phone: 13600000000
status: 停用
createTime: 2026-08-01

5. 
username: zhaoliu
name: 赵六
role: 销售人员
phone: 13500000000
status: 正常
createTime: 2026-07-31

6. 
username: sunqi
name: 孙七
role: 销售经理
phone: 13400000000
status: 正常
createTime: 2026-07-30

角色要求：

现阶段 CRM 角色规划为：
- 超级管理员
- 系统管理员
- 销售经理
- 销售人员
- 普通员工

但本测试数据至少需要确保：
- admin → 保持当前已有超级管理员角色，不要擅自改成其他角色
- zhangsan → 销售人员
- lisi → 销售经理
- wangwu → 销售人员
- zhaoliu → 销售人员
- sunqi → 销售经理

如果“销售经理”和“销售人员”角色不存在，则根据 sys_role 的真实字段结构插入。
如果已经存在，则复用，不重复插入。

然后向 sys_user_role 插入正确的用户角色关联。

密码要求：

为 zhangsan、lisi、wangwu、zhaoliu、sunqi 设置统一的开发测试密码：

123456

但是数据库中绝对不能存明文密码。

请检查当前项目 PasswordEncoder 的 BCrypt 配置，并生成 BCrypt 后的密码写入数据库。

确保这些测试账号后续能够通过当前 Spring Security 登录接口正常认证。

状态要求：

根据当前项目 sys_user.status 的真实定义生成：
- 正常用户 → 正常状态
- wangwu → 停用状态

不要自行假设 status 是字符串，如果项目实际使用 0/1 等数字，以当前项目定义为准。

其他要求：

1. SQL 必须根据当前真实表结构生成，不猜字段名。
2. 尽量保证脚本可重复执行，不因为已有角色或测试用户直接失败。
3. 不删除现有 admin。
4. 不修改 admin 密码。
5. 不修改现有权限数据。
6. 不修改生产初始化逻辑。
7. 这是开发测试数据，不要混入正式 init.sql，除非当前项目结构明确适合这样做。
8. 不执行 Git commit。
9. 不执行 Git push。
10. 先生成 SQL，不自动执行数据库写入。

完成后告诉我：

1. 三张表实际结构；
2. 新增了哪些角色；
3. 新增了哪些用户；
4. 用户与角色的关联关系；
5. BCrypt 密码如何处理；
6. 正常/停用状态在数据库中的实际值；
7. 给出完整 SQL；
8. 告诉我如何在 MySQL 中手动执行这个 SQL；
9. 给出执行后的 SELECT 验证 SQL。