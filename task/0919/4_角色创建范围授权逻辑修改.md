请重新调整当前 CRM 项目的角色创建授权设计。上一阶段基于 SUPER_ADMIN、SYSTEM_ADMIN 编码实现的角色创建范围校验不再符合最新业务规则。

**最新确定的规则：**

1. 只有 SUPER_ADMIN 可以通过 POST /api/roles 创建角色。
2. SYSTEM_ADMIN、SALES_MANAGER、SALES_STAFF 均不能创建角色，不论提交什么 roleCode。
3. SUPER_ADMIN 是系统初始化创建的根角色，不允许通过接口再次创建；其他角色编码继续遵循现有格式及唯一性规则。
4. SYSTEM_ADMIN 仍可创建用户并分配已有的普通业务角色，保持现有用户创建及角色分配逻辑不变。
5. 本次不修改角色权限配置接口，但必须检查并报告 SYSTEM_ADMIN 是否仍拥有 role:assign_permission，以及它是否可能修改系统级角色或授予高权限。

**实施要求：**

先检查当前角色权限加载机制、`@PreAuthorize` 表达式、数据库中的角色权限配置及初始化脚本，确认如何可靠地保证只有 SUPER_ADMIN 能创建角色。

在确认后，以最小改动调整 `RoleController` 和 `RoleServiceImpl`。不要仅依赖前端隐藏按钮，也不要只依赖角色编码黑名单。保留现有重复编码、参数校验及数据库唯一约束。

检查上一阶段新增的 `ForbiddenRoleCreationException`、相关异常映射和测试，删除不再需要的复杂逻辑，避免保留两套相互冲突的规则。

补充测试，覆盖 SUPER_ADMIN 创建普通角色、重复创建已有角色、禁止接口创建根角色，以及 SYSTEM_ADMIN 和销售角色使用任意合法编码创建角色均被拒绝。验证被拒绝时不会写入数据库。

执行角色专项测试及后端全量测试，报告修改文件、实际授权流程和测试结果。不要修改前端、数据库结构或用户创建逻辑，不要执行 Git commit/push。

如果需要修改现有数据库中的角色权限分配，请先给出具体 SQL 或迁移方案及影响，不要擅自修改真实数据库。
