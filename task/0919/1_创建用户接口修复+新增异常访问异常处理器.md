任务 1：
检查 POST /api/users 当前 roleIds 是否必填。

如果创建用户当前必然同时绑定角色：
将权限要求修改为同时具备
user:create
user:assign_role

如果 roleIds 实际允许为空：
不要直接机械添加双权限；
分析现有请求模型后给出最小方案，
确保“实际发生角色绑定”时必须具备 user:assign_role。

任务 2：
新增统一的 RestAccessDeniedHandler，
将已认证但授权失败统一返回 403 ProblemDetail 风格 JSON。

保持：
RestAuthenticationEntryPoint → 401
RestAccessDeniedHandler → 403

补充相应 Controller/Security 测试：
- 只有 user:create、没有 user:assign_role → 创建并绑定角色被拒绝
- 两项权限都有 → 通过授权
- 已认证缺业务权限 → 403 且响应结构符合项目规范
- 无认证 → 仍为原来的 401

不实现 SYSTEM_ADMIN 对象级限制。
不修改前端。
不修改 JWT 和 RBAC 数据结构。