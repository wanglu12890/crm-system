请基于当前 CRM 项目，完善已实现的系统管理接口的功能级权限控制。

【现状】
1. SecurityConfig 已启用 @EnableMethodSecurity。
2. POST /api/roles 已添加 @PreAuthorize("hasAuthority('role:create')")。
3. 相关 Controller 和 Service 测试已通过。
4. 多角色权限聚合及 JWT 认证保持现状。

【任务一：检查】
先检查 UserController、RoleController、PermissionController 的实际接口及现有权限编码。
确认以下权限是否存在：
user:list、user:create、role:list、role:create、role:assign_permission、permission:list。

检查现有 Controller 测试，确认测试认证用户如何配置 authorities。

【任务二：实现功能权限】
在实际存在的接口方法上添加对应授权：

GET /api/users → user:list
POST /api/users → user:create
GET /api/roles → role:list
POST /api/roles → role:create（已完成，保持不变）
GET /api/roles/{roleId}/permissions → role:assign_permission
PUT /api/roles/{roleId}/permissions → role:assign_permission
GET /api/permissions → permission:list

注意：
- 以真实 Controller 路径为准。
- 不修改现有接口路径和业务逻辑。
- 不重复添加授权注解。
- 不新增超级管理员绕过逻辑。
- 不为尚未实现的接口编写功能。

【任务三：测试】
针对各接口验证：
1. 未认证返回 401。
2. 已认证但缺少对应权限返回 403。
3. 已认证且拥有对应权限能够进入业务处理。
4. 无权限请求不得调用对应 Service。
5. 原有 Controller 和 Service 测试保持通过。

优先使用 MockMvc 和 Mock Service，不写入真实数据库。

【重要安全边界】
本次只实现功能级授权。

SYSTEM_ADMIN 的以下限制尚未实现：
- 不能分配 SUPER_ADMIN、SYSTEM_ADMIN。
- 不能修改系统级角色及其权限。
- 不能授予超出批准范围的权限。
- 不能通过修改自身账号或角色提权。

不要将本次结果描述为完整 RBAC。

特别检查 POST /api/users 是否同时执行角色绑定。
如存在，仅记录后续需要增加 user:assign_role 检查及角色分配范围校验，不在本次扩大修改范围。

【限制】
不修改数据库结构和数据。
不修改 JWT、SecurityUser、CustomUserDetailsService。
不修改前端。
不实现对象级授权。
不执行 Git commit。

【输出】
1. 实际修改文件及对应权限注解。
2. 测试命令和真实结果。
3. 仍未解决的越权风险。
4. 后续实现 SYSTEM_ADMIN 管理范围限制所涉及的 Service 方法。

完成后停止。