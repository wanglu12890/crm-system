请对当前 CRM 项目前端权限相关代码进行一次只读分析。

本次目标：
为后续实现“基于 RBAC 的前端菜单和按钮权限控制”提供改造方案。

严格限制：
- 本次只分析，不修改任何代码。
- 不新增文件。
- 不调整路由。
- 不修改组件。
- 不修改 Pinia/store。
- 不执行格式化。
- 不执行 Git commit/push。

请基于当前真实代码分析，不要根据常见 Vue 项目结构猜测。


====================
一、分析范围
====================

重点检查：

frontend/src


重点目录：

router
store
layout
sidebar
auth
system/User.vue
system/Role.vue


如果实际目录名称不同，请以项目真实结构为准。


====================
二、分析当前用户认证信息流程
====================

请梳理登录后的用户信息流转。

重点检查：

- 登录页面组件
- auth API
- token 保存工具
- Axios 请求拦截器
- auth/me 调用位置
- Pinia store 或其他状态管理
- layout 初始化逻辑


回答：

1. 用户登录成功后：
Login.vue
↓
调用哪个 API
↓
保存 token 到哪里
↓
什么时候调用 /auth/me
↓
用户信息保存在哪里


2. 当前保存了哪些用户信息？

例如：

```ts
{
 username,
 roles,
 permissions
}

说明：
roles 是否保存？
permissions 是否保存？
刷新页面后如何恢复？
是否依赖 localStorage/sessionStorage。
当前前端是否已经存在权限判断方法？

例如：

hasPermission()
hasRole()
canAccess()

如果存在：

文件位置
调用方式

如果不存在：
说明建议新增位置。

====================
三、分析菜单生成方式

检查：

layout
sidebar
router
menu 配置文件

回答：

当前侧边栏菜单在哪里定义？

例如：

静态：

const menus = [
]

还是：

动态：
后端返回菜单
        ↓
生成路由
当前菜单结构：

请整理：
系统管理
├── 用户管理
├── 角色管理
└── 权限管理

或者项目真实结构。

当前菜单是否支持权限字段？
例如：

{
 name:"权限管理",
 path:"/permission",
 permission:"permission:list"
}

如果没有：说明最小修改方案。

分析如何实现：
普通角色不显示系统管理菜单
SYSTEM_ADMIN 显示系统管理，但隐藏权限管理
SUPER_ADMIN 显示完整系统管理

不要写代码，只说明改造位置。

====================
四、分析路由权限控制

检查 router/index.ts 或相关文件。

回答：
当前路由是否有：
meta
requiresAuth
permission

例如：
{
 path:'/roles',
 meta:{
    requiresAuth:true
 }
}
当前是否实现无权限路由拦截？
例如：
用户访问：
/permission
但是没有权限。
当前行为是什么？

后续如果增加：
meta:{
 permission:"permission:list"
}

需要修改哪些地方？

====================
五、分析 User.vue

检查：system/User.vue
1.回答：当前页面有哪些按钮：
例如：
新建用户
编辑
删除
分配角色

2.哪些按钮应该根据权限控制？
根据当前 RBAC 设计：

SUPER_ADMIN：允许：新建用户
SYSTEM_ADMIN：允许：新建用户
普通销售角色：隐藏。

3.当前按钮是否有权限判断？
4.创建用户弹窗中：角色选择数据来源在哪里？

重点分析：
角色列表接口
 ↓
角色选择组件

回答后续如何实现：

SYSTEM_ADMIN 创建用户时：

显示：

SALES_MANAGER
SALES_STAFF

隐藏：

SUPER_ADMIN
SYSTEM_ADMIN
====================
六、分析 Role.vue

检查：

system/Role.vue

回答：

当前页面有哪些操作：

例如：

新建角色
配置权限
查看权限
根据当前设计：

SUPER_ADMIN：

显示：

新建角色
配置权限

SYSTEM_ADMIN：

显示：

查看角色列表

隐藏：

新建角色
配置权限

普通角色：

不进入系统管理。

当前按钮实现位置。
====================
七、输出前端权限控制改造方案

请最后输出：

1. 当前前端认证状态流程

包括：

登录
 ↓
token
 ↓
auth/me
 ↓
store
 ↓
页面
2. 当前菜单生成方式
3. 当前按钮权限实现情况
4. 需要新增或修改的文件列表

格式：

文件	修改目的
xxx	xxx
5. 推荐最小实现方案

要求：

不要重新设计前端权限体系。

优先：

复用已有 /auth/me 返回的 permissions
新增统一权限判断方法
菜单和按钮复用同一套权限数据
6. 下一步开发建议

按顺序列出：

例如：

用户权限状态管理
hasPermission 工具
Sidebar 菜单过滤
Role.vue 按钮控制
User.vue 按钮和角色选择控制
路由权限控制

完成分析后停止，不进行任何代码修改。