继续开发当前 CRM 项目的“新建用户 + 多角色绑定”功能。

重要：当前工作区已经存在我手动完成的一部分修改，这些修改尚未提交。请先完整审查现有代码和 git diff，在我的现有修改基础上继续完成，不要直接覆盖、回退或丢弃我的修改。

当前 `git status`：

```text id="rczzlq"
modified: backend/src/main/java/com/company/crm/mapper/SysUserMapper.java
modified: backend/src/main/java/com/company/crm/vo/user/UserListVO.java
modified: frontend/src/api/user.ts
modified: frontend/src/components/system/user/UserDialog.vue
modified: frontend/src/components/system/user/UserSearch.vue
modified: frontend/src/components/system/user/UserTable.vue
modified: frontend/src/types/user.ts
modified: frontend/src/views/system/User.vue
```

一、当前业务规则

系统已经明确：

```text id="8qqj41"
一个用户可以关联多个角色
```

因此用户相关前端类型已经开始从：

```ts id="wm1i5x"
role: string
```

调整为：

```ts id="9kflbz"
roleIds: string[]
```

请不要改回单角色设计。

数据库关系应继续使用现有：

```text id="86g05q"
sys_user
    ↓
sys_user_role
    ↓
sys_role
```

即：

```text id="62ulcf"
一个用户
→ 可以对应多条 sys_user_role
→ 从而关联多个角色
```

二、当前项目已有能力

已有：

```text id="1f3smu"
登录 + JWT                         ✅
GET /api/auth/me                  ✅
用户列表                          ✅
角色列表 GET /api/roles           ✅
新建角色                          ✅
权限树                            ✅
角色权限回显                      ✅
角色权限保存                      ✅
权限变更检测与幂等保存             ✅
```

现在完成：

```text id="v42jmu"
新建用户 + 多角色绑定              ← 本次
```

暂时不要实现：

```text id="8zbth5"
编辑用户
删除用户
重置密码
@EnableMethodSecurity
@PreAuthorize
动态菜单
按钮权限
角色权限差异控制
```

三、第一阶段：先审查，不要立即修改

请先检查：

```text id="h9tskc"
git diff
```

重点分析我当前这 8 个文件已经做了什么：

```text id="83t34d"
SysUserMapper.java
UserListVO.java
user.ts API
UserDialog.vue
UserSearch.vue
UserTable.vue
types/user.ts
User.vue
```

请明确：

1. 哪些修改是为了 `role → roleIds`
2. 哪些修改已经正确
3. 哪些修改不完整
4. 哪些地方存在类型或业务不一致
5. 哪些现有修改应该保留
6. 哪些地方需要在现有基础上补充

不要因为代码暂时不完整就回退我的修改。

然后继续检查现有后端：

```text id="4r6e2c"
SysUser.java
SysRole.java
SysUserRole.java（如果已有）
SysUserMapper.java
SysRoleMapper.java
SysUserRoleMapper.java（如果已有）
UserController.java
UserService.java
UserServiceImpl.java
RoleController.java
RoleService.java
SecurityUser.java
CustomUserDetailsService.java
用户相关 VO / DTO
角色相关 VO / DTO
Global / User / Role ExceptionHandler
数据库 init.sql 中 sys_user、sys_role、sys_user_role 结构
PasswordEncoder 配置
```

特别检查：

```text id="bwyvhg"
sys_user.username 是否已经有 UNIQUE 约束

sys_user_role 是否已经有：
user_id
role_id
唯一约束

SysUser 的主键是否使用 ASSIGN_ID

当前认证查询是否已经通过 sys_user_role 支持一个用户多个角色
```

必须基于实际项目结构实现，不要假设不存在的字段。

四、前端目标

完成：

```text id="vewppc"
User.vue
↓
打开 UserDialog
↓
动态加载角色列表
↓
用户填写基本信息
↓
选择一个或多个角色
↓
点击保存
↓
POST /api/users
↓
创建成功
↓
关闭 Dialog
↓
刷新用户列表
```

五、角色动态列表

优先复用现有：

```http id="azw4b3"
GET /api/roles
```

以及现有前端 role API。

不要重复创建：

```text id="vxhjgr"
getRoleId
getUserRoleOptions
```

之类功能重复的接口。

UserDialog 中角色选择应保持多选：

```text id="cf6c8j"
roleIds: string[]
```

UI：

```text id="v4ud4x"
用户看到 roleName
实际保存 role.id
```

例如：

```text id="5c7szn"
用户看到：
系统管理员
销售经理

form.roleIds：
[
  "2098342914642452481",
  "2098342914642452482"
]
```

Snowflake ID 在前端始终保持 string。

禁止：

```ts id="gwn4a8"
Number(roleId)
parseInt(roleId)
```

六、CreateUserRequest

检查现有 `frontend/src/types/user.ts`。

最终创建请求语义应类似：

```ts id="azb47r"
export interface CreateUserRequest {
  username: string
  realName: string
  password: string
  phone?: string
  status: number
  roleIds: string[]
}
```

字段名称必须根据当前实际 `sys_user` / 前端表单调整，不要凭空增加数据库不存在的字段。

重点：

```text id="vy6dpe"
roleIds 必须是 string[]
```

七、前端保存逻辑

检查当前 `UserDialog.vue` 已经写到什么程度，然后补齐保存逻辑。

建议状态：

```ts id="p5wpxj"
saving
roleLoading
roleOptions
```

保存时：

```text id="g5m1gu"
表单校验
↓
防止重复提交
↓
组装 CreateUserRequest
↓
createUser(request)
↓
成功提示
↓
关闭 Dialog
↓
emit success
↓
User.vue 重新 loadUsers()
```

失败：

```text id="1zmww7"
Dialog 保持打开
保留用户已填写数据
显示后端 ProblemDetail.detail
恢复 saving 状态
```

不要因为保存失败清空用户表单。

八、表单校验

根据项目现有 Element Plus 表单风格完善：

```text id="ngmrxe"
username 必填
realName 按现有业务规则
password 必填
phone 按现有业务规则
status 必填
roleIds 至少选择一个角色
```

如果现有 PRD 或代码允许创建无角色用户，则遵循现有规则；如果没有明确规则，本次新建用户要求至少关联一个有效角色。

密码校验不要过度设计复杂规则，保持当前项目风格。

九、后端新增 CreateUserDTO

根据现有包结构新增或完善：

```text id="x9kud7"
dto/user/CreateUserDTO.java
```

语义：

```java id="fuj99s"
username
realName
password
phone
status
List<Long> roleIds
```

添加适当 Bean Validation：

```text id="ic0ebm"
@NotBlank
@NotNull
@NotEmpty
```

具体注解根据字段业务含义决定。

注意：

前端 JSON：

```json id="0h8c87"
{
  "roleIds": [
    "2098342914642452481"
  ]
}
```

后端：

```java id="1urcr7"
List<Long> roleIds
```

让 Jackson 完成 string → Long 转换。

不要让前端转换成 JS Number。

十、POST /api/users

在现有 `UserController` 增加：

```http id="b46xzk"
POST /api/users
```

注意当前项目 context path 已经是 `/api`，Controller 路径应遵循现有代码风格，不要造成 `/api/api/users`。

使用：

```java id="jpt07r"
@Valid
@RequestBody
```

调用：

```java id="dp6doa"
userService.createUser(...)
```

成功响应请根据项目现有 REST 风格决定。

如果创建接口不需要返回完整用户，可以使用：

```text id="71bgcb"
201 Created
```

或项目当前统一的成功响应方式。

不要为了这个功能重构全局响应结构。

十一、UserService

在现有：

```text id="r2u4zc"
UserService
UserServiceImpl
```

增加：

```java id="vk2b4j"
createUser(CreateUserDTO dto)
```

并在 Service 层承担完整业务逻辑。

Controller 不直接操作 Mapper。

十二、createUser 后端业务流程

建议严格按照：

```text id="e82m6d"
① 获取当前 operator
② 校验 username 唯一
③ roleIds 去重
④ 校验所有角色合法
⑤ BCrypt 加密密码
⑥ DTO → SysUser
⑦ 补充系统字段
⑧ INSERT sys_user
⑨ 获得生成后的 userId
⑩ INSERT sys_user_role 多条关系
⑪ 事务提交
⑫ INFO 成功日志
```

十三、用户名唯一性

Service 层首先校验：

```text id="ajotmt"
username 是否已经存在
```

条件根据现有软删除设计决定，例如：

```text id="3z56ol"
username = ?
deleted = 0
```

如果重复：

```text id="5r3mmu"
409 Conflict
```

建议业务错误码：

```text id="h1d4lc"
USERNAME_ALREADY_EXISTS
```

如果数据库已经存在 username UNIQUE 约束，继续保留。

同时考虑并发竞争：

```text id="t0wfxl"
两个请求同时查询不存在
↓
同时 INSERT
```

如果数据库 UNIQUE 最终抛出 `DuplicateKeyException`，应转换成相同或合理的业务异常。

不要仅依赖前置 select。

十四、角色校验

`roleIds` 首先去重。

例如：

```text id="3ux8zd"
[A, A, B]
→
[A, B]
```

然后一次性查询所有角色。

所有待绑定角色必须满足：

```text id="xyig8h"
id IN roleIds
deleted = 0
status = 1
```

如果提交：

```text id="zyry1m"
[A, B, X]
```

只有 A、B 有效，则整个创建请求失败。

禁止：

```text id="o6mmlo"
静默过滤 X
然后继续创建用户
```

避免用户以为 X 已绑定，实际上没有。

异常可以使用：

```text id="cfutl1"
INVALID_ROLE
```

或遵循当前项目已有异常命名规范。

十五、密码处理

这是本次重点。

绝对禁止：

```java id="uqqgqk"
user.setPassword(dto.getPassword());
```

必须复用当前 Spring Security 已配置的：

```java id="o7nmcg"
PasswordEncoder
```

例如：

```java id="a4vv8f"
user.setPassword(passwordEncoder.encode(dto.getPassword()));
```

不要自己 new BCryptPasswordEncoder，如果项目已经有 Bean。

不要创建第二套密码编码规则。

创建的新用户必须能够直接通过现有：

```text id="gmy4je"
DaoAuthenticationProvider
PasswordEncoder.matches()
```

登录。

十六、SysUser 实体组装

复用现有：

```text id="s3im4p"
SysUser.java
```

不要再创建重复的 UserEntity。

根据当前实体和数据库字段补充：

```text id="4dyc77"
username
realName
passwordHash / password（按实际字段）
phone
status
createdBy
updatedBy
createdAt
updatedAt
deleted
version
```

只设置数据库实际存在且项目当前要求维护的字段。

如果 ID 已使用：

```java id="f7h6gn"
@TableId(type = IdType.ASSIGN_ID)
```

继续由 MyBatis-Plus 生成 Snowflake ID。

不要自己生成另一套 ID。

十七、SysUserRole

检查是否已有：

```text id="yxwyzl"
SysUserRole.java
SysUserRoleMapper.java
```

如果已有，直接复用。

如果不存在，则根据现有 `sys_user_role` 表新增最小必要实体与 Mapper。

关系：

```text id="gjnj89"
userId
roleId
```

如果表还有：

```text id="6qzoyr"
id
create_time
```

按照实际数据库结构处理。

不要修改数据库结构，除非发现当前表结构根本无法支持现有设计；这种情况先报告，不要擅自迁移。

十八、插入用户角色关系

用户创建成功取得：

```text id="21wvsa"
user.getId()
```

之后：

```text id="mm6t53"
for each roleId
→ INSERT sys_user_role
```

当前角色数量很少，可以逐条 insert。

本次不需要为了性能增加批量插入。

十九、事务

`createUser()` 必须：

```java id="rhdnxg"
@Transactional
```

因为：

```text id="sz1rlj"
INSERT sys_user
+
INSERT sys_user_role
```

属于一个完整业务操作。

必须保证：

```text id="dw73l6"
用户插入成功
角色关系第 N 条插入失败
↓
整个事务 rollback
↓
sys_user 也不能留下半成品记录
```

不要在 Controller 上加事务。

二十、日志

继续遵守当前项目日志规范。

成功创建：

```text id="y2bfkz"
INFO
operator
userId
username
roleCount
```

业务拒绝：

```text id="pj8of3"
WARN
用户名重复
角色非法
```

不要记录：

```text id="1h2hm9"
password
password hash
JWT
Authorization
数据库密码
```

异常日志避免与全局异常处理重复打印完整堆栈。

二十一、用户列表的多角色显示

我已经修改了：

```text id="vegtlo"
SysUserMapper.java
UserListVO.java
UserTable.vue
UserSearch.vue
types/user.ts
```

请重点检查这些修改。

目标是用户列表不能再假设：

```text id="g7f52e"
一个用户只有一个 role
```

而应该正确表示：

```text id="rl92u5"
一个用户
→ 多个角色
```

例如：

```text id="m1xfqw"
张三
系统管理员 / 销售经理
```

具体 UI 可以沿用当前设计，例如多个 Tag。

如果我现有 Mapper 已经实现角色聚合查询，请优先保留并修正，不要无理由推翻重写。

重点检查 SQL 是否会因为：

```text id="2bg1u4"
sys_user
JOIN sys_user_role
JOIN sys_role
```

导致一个用户被展开成多行，从而破坏用户列表分页。

如果当前采用 SQL 聚合或后续组装，请确保：

```text id="f9tw3v"
一个用户最终仍对应一条 UserListVO
```

二十二、不要破坏认证链路

检查：

```text id="cn85z3"
CustomUserDetailsService
SecurityUser
/auth/me
```

确认现有认证是否已经支持：

```text id="ph2f4n"
一个用户多个角色
```

创建新用户后，登录应该能够得到：

```text id="fgigf4"
roles = [
  "SYSTEM_ADMIN",
  ...
]
```

权限应该由这些角色关联的权限正确汇总。

如果现有认证已经支持，不修改。

如果发现仍然写死单角色，只报告问题并做实现本次功能所必需的最小修改，不要扩大为权限控制重构。

二十三、异常处理

根据项目现有结构补充必要异常，例如：

```text id="0y4r77"
DuplicateUsernameException
InvalidUserRoleException
```

最终使用现有 ProblemDetail 风格返回。

建议语义：

```text id="i1uy5f"
username 重复
→ HTTP 409
→ USERNAME_ALREADY_EXISTS

角色不存在 / 停用 / 非法
→ HTTP 400
→ INVALID_ROLE
```

具体错误码名称遵循当前项目已有规范。

二十四、测试

至少增加以下后端测试：

1. 正常创建单角色用户

```text id="w1uy2v"
roleIds=[A]
→ sys_user insert
→ sys_user_role insert A
```

2. 正常创建多角色用户

```text id="gpbmrj"
roleIds=[A,B]
→ user 一条
→ user_role 两条
```

3. roleIds 重复

```text id="b2a2i8"
[A,A,B]
→ 最终只绑定 A,B
```

4. username 已存在

```text id="28s58r"
→ 创建失败
→ 不 insert user
→ 不 insert user_role
```

5. 包含非法角色

```text id="bnudcc"
[A,X]
→ 整个请求失败
→ 不创建用户
```

6. 包含停用角色

```text id="obcrr8"
→ 整个请求失败
```

7. 密码经过 PasswordEncoder.encode()

不要保存明文。

8. user_role 插入过程中异常

确认 `createUser()` 有 `@Transactional`。

如果当前测试基础设施无法做真实 DB rollback 集成测试，应明确报告：

```text id="zvnwmj"
事务注解和异常传播已验证
但未进行真实数据库 rollback read-back 测试
```

不要声称已经验证真实 rollback。

9. Controller：

验证前端 Snowflake string：

```json id="fhxlpm"
"roleIds": ["2098342914642452481"]
```

能够被 Jackson 正确反序列化成：

```java id="tqf2za"
List<Long>
```

二十五、前端验证

至少执行项目现有：

```text id="qv7p0e"
vue-tsc
tsc
Vite production build
```

并检查：

```text id="kj9mxr"
UserDialog 多角色类型
CreateUserRequest
UserTable
UserSearch
User.vue
```

没有残留：

```ts id="rfr8kx"
role: string
```

这种与新业务规则冲突的旧类型。

注意：不要机械删除所有名为 role 的变量；只处理确实表示“用户关联角色”的旧单角色业务字段。

二十六、后端验证

运行：

```text id="y6vwky"
UserServiceImplTest
UserControllerTest
相关 Mapper 测试（如已有）
完整 Maven test
```

如果再次遇到 Windows：

```text id="mry2qa"
target/classes/application-local.yml
```

文件锁，请明确报告，不要擅自结束用户进程或删除不确定文件。

二十七、最终人工验证链路

代码完成后，请给出人工验证步骤，至少包括：

```text id="zx0lyb"
SUPER_ADMIN 登录
↓
打开用户管理
↓
点击新建用户
↓
角色列表动态加载
↓
选择 SYSTEM_ADMIN
↓
填写用户信息
↓
保存
↓
POST /api/users 成功
↓
用户列表刷新
↓
新用户显示 SYSTEM_ADMIN
↓
退出 SUPER_ADMIN
↓
使用新用户登录
↓
GET /api/auth/me
↓
roles 包含 SYSTEM_ADMIN
↓
permissions 与 SYSTEM_ADMIN 已配置权限一致
```

再补充一个多角色测试：

```text id="4llb4c"
创建用户
↓
同时选择两个角色
↓
保存
↓
sys_user 一条记录
↓
sys_user_role 两条记录
↓
用户列表显示两个角色
↓
登录后 /auth/me 返回两个角色
```

二十八、完成后输出详细报告

请按以下结构报告：

```text id="1lswyq"
1. 开始前 git diff 审查结果
2. 我原来 8 个文件分别修改了什么
3. 哪些原有修改被保留
4. 哪些原有修改被修正
5. 新增了哪些文件
6. 最终修改了哪些文件

前端：
7. UserDialog 如何动态加载角色
8. 多角色如何保存到 form.roleIds
9. CreateUserRequest 最终结构
10. 保存按钮完整调用链
11. 创建失败如何处理
12. 创建成功如何刷新列表
13. 用户列表如何显示多个角色

后端：
14. POST /api/users 调用链
15. CreateUserDTO 最终结构
16. username 如何校验唯一
17. roleIds 如何去重
18. 角色如何校验
19. 密码如何加密
20. SysUser 如何生成 ID
21. sys_user_role 如何插入多角色
22. @Transactional 在哪里
23. 角色关系插入失败时如何保证一致性
24. 日志记录了什么
25. 哪些敏感信息明确没有记录

兼容：
26. Snowflake ID 前后端如何避免精度丢失
27. 一个用户多个角色是否影响现有用户列表
28. 是否影响 CustomUserDetailsService
29. 是否影响 /auth/me

测试：
30. UserServiceImplTest 结果
31. UserControllerTest 结果
32. 完整 Maven test 结果
33. 前端 type check/build 结果
34. 尚未真实验证的内容
35. 建议我接下来进行的人工测试步骤
```

不要执行：

```text id="ncb13k"
git add
git commit
git push
```

完成代码和测试即可。
