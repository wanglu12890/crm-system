你现在作为一名资深Java架构师，帮我设计并创建一个标准企业级CRM系统项目。

项目名称：crm-system

技术栈要求：
后端：
- Spring Boot 3.x
- Java 17
- Maven
- MySQL 8.x
- MyBatis-Plus
- Spring Security
- JWT认证
- Lombok
- RESTful API设计

前端：
- Vue3
- Vite
- TypeScript
- Vue Router
- Pinia
- Axios
- Element Plus

项目类型：
  企业B端CRM客户管理系统。请严格按照真实企业项目开发规范设计，不要生成简单Demo。
  第一阶段只进行项目架构设计，不编写业务代码。

请完成以下内容：
1. 创建完整前后端分离项目目录结构
要求包含：
crm-system
├── backend│
└── frontend

其中backend需要包含：
- controller
- service
- service.impl
- mapper
- entity
- dto
- vo
- config
- security
- exception
- utils
- common

frontend需要包含：
- api
- assets
- components
- layouts
- router
- stores
- views
- utils
- types

2. 输出每个目录的作用说明
说明：
- 为什么这样划分
- 企业项目为什么采用这种结构

3. 设计后端代码分层架构
详细说明：
Controller层职责
Service层职责
Mapper层职责
Entity层职责
DTO和VO区别

前端请求到数据库查询的完整数据流。

4. 设计CRM核心业务模块
包括：
用户认证模块
客户管理模块
联系人管理模块
线索管理模块
商机管理模块
跟进记录模块
合同管理模块
数据统计模块
系统管理模块


5. 设计MySQL数据库结构
输出主要数据表：
用户表
角色权限表
客户表
联系人表
线索表
商机表
跟进记录表
合同表

包含：
字段设计
字段类型
表关系


6. 设计RESTful API规范
例如：
登录：POST /api/auth/login
客户：
GET /api/customer/list
POST /api/customer/add
PUT /api/customer/update
DELETE /api/customer/{id}

说明：
请求方式
接口职责
前后端如何交互。

7. 创建Maven项目配置说明

包括：
pom.xml核心依赖
Spring Boot版本
Java版本
MyBatis-Plus配置
MySQL驱动
Spring Security配置
JWT依赖。

8. 创建Vue项目配置说明

包括：
Vite配置
路由设计
状态管理设计
Axios请求封装。


9. 输出项目启动流程
包括：
后端启动：
mvn clean install
mvn spring-boot:run

前端启动：
npm install
npm run dev

数据库初始化流程。

10. 创建README.md
包含：
项目介绍
技术栈
目录结构
环境要求
启动方法
开发规范。

注意：
- 不要一次生成大量业务代码。
- 当前目标是建立一个规范企业级项目骨架。
- 所有设计必须符合真实互联网公司B端软件开发规范。